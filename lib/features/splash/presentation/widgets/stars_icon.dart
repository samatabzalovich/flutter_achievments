 import 'package:flutter/material.dart';

class StarsIcon extends StatelessWidget {
  const StarsIcon({super.key});

  @override
  Widget build(BuildContext context) {
    return Image.asset(
      'assets/icons/Stars_group.png',
    );
  }
}